export const form = document.querySelector('[data-form-authorization]');
export const authorizationModalWindow = document.querySelector('[data-modal-authorization]');

export const errorMessage = 'Error! Invalid email or password. Check it';
export const successMessage = 'Successfully authenticated! Welcome!';